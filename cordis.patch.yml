# dsh-allow bundle 层:一个宿主插件行(浏览器半由 package.json 的 dsh.client 声明注入)。
# 安装: dsh plugin --profile web add link:<本目录>
# 可选配置:
#   - id: dsh-allow
#     config:
#       rulesFile: /path/to/dsh-allow.json   # 默认 $DSH_HOME/dsh-allow.json
- insert:
    - id: dsh-allow
      name: dsh-allow
