# dsh-allow bundle 层:一个宿主插件行(只有宿主半,弹窗走 user-questions 通道)。
# 安装: dsh plugin --profile web add link:<本目录>
# 可选配置:
#   - id: dsh-allow
#     config:
#       rulesFile: /path/to/dsh-allow.json   # 默认 $DSH_HOME/dsh-allow.json
- insert:
    - id: dsh-allow
      name: dsh-allow
